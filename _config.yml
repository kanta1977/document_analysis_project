title: "Whose Words Survive?"
description: "What a Reddit TL;DR actually is — a Document Analysis project on self-summarization across communities."
show_downloads: true
plugins:
  - jekyll-relative-links
relative_links:
  enabled: true
  collections: true
include:
  - README.md
  - REPORT.md

theme: jekyll-theme-minimal
